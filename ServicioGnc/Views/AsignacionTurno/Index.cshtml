@model IEnumerable<ServicioGnc.Models.AsignacionTurno>

@{
    ViewBag.Title = "Asignación de turnos";
}

<h2>Asignación de turnos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr class="warning">
        <th>
            @Html.DisplayNameFor(model => model.Turno.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persona.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Turno.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AsignacionTurnoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AsignacionTurnoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AsignacionTurnoId })
        </td>
    </tr>
}

</table>
