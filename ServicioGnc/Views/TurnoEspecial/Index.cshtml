@model IEnumerable<ServicioGnc.Models.TurnoEspecial>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Feriado.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persona.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Feriado.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TurnoEspecialId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TurnoEspecialId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TurnoEspecialId })
        </td>
    </tr>
}

</table>
